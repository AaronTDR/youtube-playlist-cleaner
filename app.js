// Gets the videos from the list
const videos = document.querySelectorAll(
  ".yt-simple-endpoint.style-scope.ytd-playlist-video-renderer"
);
// Gets the dropdown menu
const menuButton = document.querySelector("#contents #menu #button");

const deleteButton = document.querySelector(
  "tp-yt-iron-dropdown #contentWrapper ytd-menu-popup-renderer #items > :nth-child(3)"
);
